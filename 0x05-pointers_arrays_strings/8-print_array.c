#include "main.h"
/**
* print_array -> prints elements
* @a: parameter1
* @n: parameter2
* Return: elements of an array of integers
*/
void print_array(int *a, int n)
{
	int [n];
	char j;

	for (j = 0; j < n; j++)
	{
		printf("Element[%d] = %d, \n", j, [j]);
	}
	return (0);
}

